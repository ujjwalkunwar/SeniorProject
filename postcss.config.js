/**
 * PostCSS configuration object.
 * 
 * @type {Object}
 * @property {Object} plugins - Object containing configurations for PostCSS plugins.
 * @property {Object} plugins.tailwindcss - Configuration for the Tailwind CSS plugin.
 * @property {Object} plugins.autoprefixer - Configuration for the Autoprefixer plugin.
 */

module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};
