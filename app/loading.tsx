import Loader from './components/Loader';
import React from 'react';

/**
 * Component for displaying a loading indicator.
 * 
 * @returns {JSX.Element} JSX element representing the loading indicator.
 */
const Loading = () => {
  return <Loader />;
};

export default Loading;